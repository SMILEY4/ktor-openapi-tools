{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Ktor OpenAPI Tools","text":"<p>A collection of libraries to simplify API documentation and exploration for Ktor applications. Designed to be non-invasive, they integrate seamlessly with applications without requiring immediate change to existing code while being highly customizable to fit every use case.</p> <ul> <li> <p> OpenAPI</p> <p><code>ktor-openapi</code></p> <p>Add documentation to Ktor routes and automatically generate OpenAPI specifications.</p> <p> Get Started</p> </li> <li> <p> Swagger UI</p> <p><code>ktor-swagger-ui</code></p> <p>Serve an interactive Swagger UI for easy API exploration and testing</p> <p> Get Started</p> </li> <li> <p> Redoc</p> <p><code>ktor-redoc</code></p> <p>Serve an interactive ReDoc page for easy API exploration and testing</p> <p> Get Started</p> </li> </ul>"},{"location":"openapi/","title":"OpenAPI","text":"<p>Ktor plugin to automatically generate OpenAPI specifications from routes. Additional information can be gradually added to existing routes without requiring major changes to existing code.</p>"},{"location":"openapi/#features","title":"Features","text":"<ul> <li>Extends existing Ktor DSL</li> <li>No immediate change to code required</li> <li>Support for Type-safe routing / Resources plugin</li> <li>Document webhooks and (limited) options for server-sent events</li> <li>Covers (almost) complete OpenAPI 3.1.0 Specification</li> <li>Automatically generates json schemas from kotlin types<ul> <li>Out-of-the-box support for type parameters, inheritance, collections, etc</li> <li>Usable with reflection or kotlinx.serialization</li> <li>Supports Jackson, Swagger, Javax and Jakarta annotations</li> <li>Highly configurable and customizable</li> </ul> </li> </ul>"},{"location":"openapi/#example","title":"Example","text":"<pre><code>install(OpenApi) //(1)!\n\nrouting {\n\n    route(\"api.json\") {\n        openApi() //(2)!\n    }\n\n    get(\"example\", {\n        description = \"An example route\" //(3)!\n        response {\n            HttpStatusCode.OK to {\n                description = \"A success response\"\n                body&lt;String&gt;()\n            }\n        }\n    }) {\n        call.respondText(\"Hello World!\") //(4)!\n    }\n}\n</code></pre> <ol> <li>Install and configure the OpenAPI plugin.</li> <li>Create a route to expose the OpenAPI specification file at <code>/api.json</code>.</li> <li>Add (optional) information to the route, e.g. a description and responses and response bodies.</li> <li>Handle requests as usual.</li> </ol>"},{"location":"openapi/documenting_routes/","title":"Documenting Routes","text":"<p>The <code>ktor-openapi</code> plugin provides functions to create documented <code>get</code>, <code>post</code>, <code>put</code>, <code>delete</code>, <code>patch</code>, <code>options</code>, <code>head</code>, <code>route</code> and <code>method</code> routes. These work exactly the same as their respective base Ktor functions, but allow OpenAPI documentation to be added. Routes provided by the plugin and routes from Ktor can be mixed and allow for a gradual enhancement of the api with documentation.</p> Adding Documentation to Routes <pre><code>import io.github.smiley4.ktoropenapi.get //(1)!\n\nget(\"hello\", {\n    description = \"A Hello-World route\" //(2)!\n    request { //(3)!\n        queryParameter&lt;String&gt;(\"name\") { //(4)!\n            description = \"the name to greet\"\n        }\n    }\n    response { //(5)!\n        code(HttpStatusCode.OK) { //(6)!\n            description = \"successful request\"\n        }\n    }\n}) {\n    call.respond(HttpStatusCode.NotImplemented, Unit) //(7)!\n}\n</code></pre> <ol> <li>Replace <code>io.ktor.server.routing.get</code> with <code>io.github.smiley4.ktoropenapi.get</code>. Same for other http methods.</li> <li>Add a description to the route.</li> <li>Document request information, e.g. parameters, headers, bodies, etc.</li> <li>Specify a query parameter <code>name</code> of type <code>String</code> and add a description.</li> <li>Document possible responses, response bodies, headers, etc.</li> <li>Specify a possible response with the status code <code>200 OK</code> and add a description.</li> <li>Handle requests as usual.</li> </ol> Nested Documentation <p>Documentation can be added at any level and documentation of parent and child routes are merged together, with priority given to the deepest route.</p> <pre><code>route(\"parent\", {\n    description = \"Common description for all routes.\" //(1)!\n}) {\n\n    get(\"child1\") { //(2)!\n        call.respond(HttpStatusCode.NotImplemented, Unit)\n    }\n\n    get(\"child2\", {\n        description = \"Specific description of child 2.\" //(3)!\n    }) {\n        call.respond(HttpStatusCode.NotImplemented, Unit)\n    }\n\n}\n</code></pre> <ol> <li>The route <code>/parent</code> has a description added to it. This description applies to this route and all child routes, as long as it is not overwritten by any deeper route.</li> <li><code>/parent/child1</code> does not have any own documentation and only the information of the parent routes applies.</li> <li><code>/parent/child2</code> specifies its own description and overwrites the description of the parent route.</li> </ol> API Reference <p> API Reference for more information on available documentation options for routes.</p>"},{"location":"openapi/documenting_routes/#documenting-requests","title":"Documenting Requests","text":"<p>Information about a request is added in the <code>request</code>-block of a route-documentation.</p> Adding Information About a Request <pre><code>post(\"entity/{id}\", {\n    description = \"Create a new entity.\"\n    request { //(1)!\n        pathParameter&lt;String&gt;(\"id\") { //(2)!\n            description = \"The id of the entity to create.\"\n            example(\"default\") {\n                value = UUID.randomUUID().toString()\n            }\n        }\n        body&lt;NewEntity&gt; { //(3)!\n            description = \"The data of the new entity to create.\"\n            required = true\n            example(\"default\") {\n                value = NewEntity()\n            }\n        }\n    }\n}) {\n    call.respond(HttpStatusCode.NotImplemented, Unit)\n}\n</code></pre> <ol> <li>Add all information about http requests in the <code>request</code>-block..</li> <li>Add documentation about the path parameter with the name <code>id</code> of type <code>String</code>.</li> <li>Add documentation about the request body. The body must be present (<code>required = true</code>) and be of type <code>NewEntity</code>.</li> </ol> API Reference <p> API Reference for more information on available documentation options for requests.</p>"},{"location":"openapi/documenting_routes/#documenting-responses","title":"Documenting Responses","text":"<p>Information about possible responses is added in the <code>response</code>-block of a route-documentation.</p> Adding Information About Responses <pre><code>get(\"entities\", {\n    description = \"Returns all entities from the system that the user has access to.\"\n    response {\n        code(HttpStatusCode.OK) { //(1)!\n            body&lt;List&lt;Entity&gt;&gt; { //(2)!\n                required = true\n                description = \"The list of available entities.\"\n            }\n        }\n        code(HttpStatusCode.Unauthorized) {\n            description = \"The user is not authorized to view the list of entities.\"\n        }\n        default { //(3)!\n            body&lt;ErrorModel&gt; {\n                description = \"An unexpected error occurred\"\n            }\n        }\n    }\n}) {\n    call.respond(HttpStatusCode.NotImplemented, Unit)\n}\n</code></pre> <ol> <li>Add information for a response with a <code>200 OK</code> status code.</li> <li>Specify the body of this response. The body must be present (<code>required = true</code>) and be of type <code>List&lt;Entity&gt;</code>.</li> <li>Document the default response.\"</li> </ol> Alternative Syntax for Status Codes <p>There are two alternative ways of specifying response codes. Both are valid options and functionally the same.</p> code(...) {}... to {} <pre><code>response {\n    code(HttpStatusCode.OK) {\n        //...\n    }\n}\n</code></pre> <pre><code>response {\n    HttpStatusCode.OK to {\n        //...\n    }\n}\n</code></pre> API Reference <p> API Reference for more information on available documentation options for requests.</p>"},{"location":"openapi/documenting_routes/#documenting-webhooks","title":"Documenting Webhooks","text":"<p>Even though Webhooks are not a part of a Ktor server application, they can be added to the OpenAPI specification for documentation purposes.</p> Documenting Webhooks <pre><code>import io.github.smiley4.ktoropenapi.webhook\n\nrouting {\n    webhook(HttpMethod.Post, \"alert\") { //(1)!\n        description = \"Notify the registered URL with details of an upcoming concert\" //(2)!\n        request {\n            body&lt;String&gt; {\n                mediaTypes(ContentType.Text.Plain)\n                required = true\n            }\n        }\n    }   \n}\n</code></pre> <ol> <li>Add the webhook using the route extension function. This does not create any callable Ktor route and is only for documentation purposes. </li> <li>Document the webhook route like any other route.</li> </ol> More Information <p> API Reference</p>"},{"location":"openapi/documenting_routes/#documenting-sse-routes","title":"Documenting SSE-Routes","text":"<p>For discoverability purposes, server-sent event routes can be added to the OpenAPI specification. Ktor SSE routes do not have an own extension function allowing for api documentation to be added. Instead, they have to be wrapped in a parent route containing the desired documentation.</p> <pre><code>routing {\n    route({\n        description = \"Send notifications to the client.\" //(1)!\n    }) {\n        sse(\"/events\") { /*...*/ } //(2)!\n    }\n}\n</code></pre> <ol> <li>The parent route with the desired api documentation.</li> <li>The SSE route.</li> </ol> Warning <p>Support for server-sent events in OpenAPI and this plugin is limited. Even though all documentation options are technically available, not all may work or have the intended effect. </p>"},{"location":"openapi/example_encoding/","title":"Example Encoding","text":"<p>Example objects are automatically serialized (as json) and added to the OpenAPI specification. The encoding of example values can be customized in the plugin configuration. If no encoder is specified, the example value will be encoded by swagger.</p>"},{"location":"openapi/example_encoding/#pre-defined-example-encoders","title":"Pre-Defined Example Encoders","text":"SwaggerKotlinx.Serialization <p>Explicitly use the internal swagger example encoder. </p> <pre><code>install(OpenApi) {\n    examples {\n        encoder = ExampleEncoder.internal()\n    }\n}\n</code></pre> More Information <p> API Reference</p> <p>Use kotlinx.serialization to encode example values.</p> <pre><code>install(OpenApi) {\n    examples {\n        encoder = ExampleEncoder.kotlinx()\n    }\n}\n</code></pre> More Information <p> API Reference</p>"},{"location":"openapi/example_encoding/#custom-example-encoder","title":"Custom Example Encoder","text":"<p>The example encoder can be completely replaced by an own implementation.</p> <pre><code>install(OpenApi) {\n    examples {\n        encoder { type, example -&gt; //(1)!\n            TOOD() //(2)!\n        }\n    }\n}\n</code></pre> <ol> <li>Input of the example encoding function is a <code>io.github.smiley4.ktoropenapi.config.TypeDescriptor</code> with information about the type/schema and the actual value of the example.</li> <li>Encode/Transform the example value and return the result.</li> </ol> Custom \"toString()\" Example Encoder <pre><code>install(OpenApi) {\n    examples {\n        encoder { type, example -&gt;\n            example.toString() //(1)!\n        }\n    }\n}\n</code></pre> <ol> <li>Always encode and embed the example value as a raw string.</li> </ol>"},{"location":"openapi/getting_started/","title":"Getting Started","text":""},{"location":"openapi/getting_started/#add-dependency","title":"Add Dependency","text":"<p>To generate OpenAPI specifications, you need to include the <code>ktor-openapi</code> artifact in the build script. All artifacts are published to Maven Central.</p> Gradle (Kotlin)GradleMaven <pre><code>implementation(\"io.github.smiley4:ktor-openapi:$version\")\n</code></pre> <pre><code>implementation 'io.github.smiley4:ktor-openapi:$version'\n</code></pre> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;io.github.smiley4&lt;/groupId&gt;\n    &lt;artifactId&gt;ktor-openapi&lt;/artifactId&gt;\n    &lt;version&gt;${version}&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre> Ktor Compatibility and Previous Versions <p>This project as been split into multiple projects starting with version 5.0.Versions up to 5.0 are called <code>ktor-swagger-ui</code> instead of <code>ktor-openapi</code>.</p> Ktor Plugin Version Project Name 2.x up to 3.x <code>ktor-swagger-ui</code> 3.x 4.x <code>ktor-swagger-ui</code> 3.x 5.x <code>ktor-openapi</code>"},{"location":"openapi/getting_started/#install-openapi","title":"Install OpenAPI","text":"<pre><code>install(OpenApi) { //(1)!\n    //...(2)\n}\n</code></pre> <ol> <li>Install the \"OpenAPI\" plugin to the application.</li> <li>Add additional plugin configuration here.</li> </ol> More Information <p> Plugin Configuration</p> <p> API Reference</p>"},{"location":"openapi/getting_started/#exposing-openapi-specification","title":"Exposing OpenAPI Specification","text":"<pre><code>routing {\n    route(\"api.json\") { //(1)!\n        openApi() //(2)!\n    }\n}\n</code></pre> <ol> <li>Create a new route to expose the OpenAPI specification file at <code>api.json</code>.</li> <li>Expose the OpenAPI specification.</li> </ol> More Information <p> Multiple OpenAPI Specifications</p> <p> API Reference</p>"},{"location":"openapi/getting_started/#documenting-routes","title":"Documenting Routes","text":"<pre><code>import io.github.smiley4.ktoropenapi.get //(1)!\n\nget(\"hello\", { //(2)!\n    description = \"A Hello-World route\" //(3)!\n    response {\n        HttpStatusCode.OK to { //(4)!\n            description = \"A success response\"\n            body&lt;String&gt;() //(5)!\n        }\n    }\n    //...\n}) {\n    call.respondText(\"Hello World!\") //(6)!\n}\n</code></pre> <ol> <li>Replace <code>io.ktor.server.routing.get</code> with <code>io.github.smiley4.ktoropenapi.get</code>. Same for other http methods.</li> <li>Enrich <code>/hello</code> route with additional information.</li> <li>Add a description to the route.</li> <li>Document the different possible responses.</li> <li>Specify the response body type. The schema for the type is generated automatically.</li> <li>Handle requests as usual.</li> </ol> More Information <p> Documenting Routes</p> <p> Documentation with Type-safe Routing</p> <p> API Reference</p>"},{"location":"openapi/handling_types_schemas_examples/","title":"Handling Types, Schemas and Examples","text":""},{"location":"openapi/handling_types_schemas_examples/#local-types-and-schemas","title":"Local Types and Schemas","text":"<p>Usually, schemas for request and response bodies, headers and parameters are specified directly at the route and then automatically collected during generation of the OpenAPI specification.</p> <p>There are three main ways of specifying the schema of any object directly at the route:</p> Type ParameterKTypeSwagger Schema <p>The type can be specified via a type parameter.The schema is then generated automatically. <pre><code>body&lt;ExampleData&gt; { /*...*/ }\n</code></pre> <pre><code>queryParameter&lt;String&gt;(\"example\") { /*...*/ }\n</code></pre> <pre><code>headerParameter&lt;List&lt;String&gt;&gt;(\"X-Example\") { /*...*/ }\n</code></pre> etc</p> <p>The type can be passed as a parameter of type KType.The schema is then generated automatically. <pre><code>body(typeof&lt;ExampleData&gt;) { /*...*/ }\n</code></pre> <pre><code>queryParameter(\"example\", typeof&lt;String&gt;) { /*...*/ }\n</code></pre> <pre><code>headerParameter(\"X-Example\", typeof&lt;List&lt;String&gt;&gt;) { /*...*/ }\n</code></pre> etc</p> <p>The type can be passed as a parameter of type <code>io.swagger.v3.oas.models.media.Schema</code>.The given schema is then used as is. <pre><code>body(Schema&lt;Any&gt;().apply {\n    types = setOf(\"object\")\n    title = \"ExampleData\"\n    //...\n}) { /*...*/ }\n</code></pre> <pre><code>queryParameter(\n    \"example\",\n    Schema&lt;Any&gt;().apply {\n        types = setOf(\"string\")\n        //...\n    }\n) { /*...*/ }\n</code></pre> <pre><code>headerParameter(\n    \"X-Example\",\n    Schema&lt;Any&gt;().apply {\n        types = setOf(\"string\")\n        //...\n    }\n) { /*...*/ }\n</code></pre> etc</p>"},{"location":"openapi/handling_types_schemas_examples/#global-types-and-schemas","title":"Global Types and Schemas","text":"<p>In addition to types and schema specified directly at the routes, global schemas can be defined in the schema section of the plugin configuration that can then be used and shared by any route documentation. The ids of the schemas must be unique.</p> Defining Global Schemas <pre><code>install(OpenApi) {\n    schemas {\n\n        schema&lt;ExampleData&gt;(\"example-data\") //(1)!\n\n        schema(\"string\", typeOf&lt;String&gt;()) //(2)!\n\n        schema(\"integer\", Schema&lt;Any&gt;().apply { //(3)!\n            types = setOf(\"number\")\n            format = \"int32\"\n        })\n\n    }\n}\n</code></pre> <ol> <li>Define a new global schema with id <code>example-data</code> via the type parameter of <code>schema</code>. The actual schema for the type is generated automatically.</li> <li>Define a new global schema with id <code>string</code> by passing a <code>KType</code> to <code>schema</code>. The actual schema for the type is generated automatically.</li> <li>Define a new global schema with id <code>integer</code> by passing a <code>io.swagger.v3.oas.models.media.Schema</code> to <code>schema</code>.</li> </ol> More Information <p> API Reference</p> <p>Global schemas can be referenced by any route documentation by their schema id using the <code>ref</code>-function</p> Referencing Global Schemas <pre><code>routing {\n\n    get({\n        request {\n            queryParameter(\"limit\", ref(\"integer\")) //(1)!\n            headerParameter(\"X-Custom\", ref(\"string\")) //(2)!\n            body(ref(\"example-1\")) //(3)!\n        }\n    }) {\n        call.respond(HttpStatusCode.NotImplemented, Unit)\n    }\n\n}\n</code></pre> <ol> <li>Use the global schema with id <code>integer</code> as the schema for the query parameter.</li> <li>Use the global schema with id <code>string</code> as the schema for the header parameter.</li> <li>Use the global schema with id <code>example-1</code> as the request body schema.</li> </ol>"},{"location":"openapi/handling_types_schemas_examples/#composite-schemas","title":"Composite Schemas","text":"<p>Schemas and types can be combined to created variations without having to create new classes.</p>"},{"location":"openapi/handling_types_schemas_examples/#array","title":"array","text":"<p>Creates a schema of an array containing items of the specified type or schema.</p> <pre><code>import io.github.smiley4.ktoropenapi.config.array\n\nbody(array&lt;ExampleData&gt;()) //(1)!\n</code></pre> <ol> <li>The schema of the body is an array of <code>ExampleData</code>.</li> </ol>"},{"location":"openapi/handling_types_schemas_examples/#anyof","title":"anyOf","text":"<p>Creates a schema that describes any one of the specified types or schemas.</p> <pre><code>import io.github.smiley4.ktoropenapi.config.anyOf\n\nbody(anyOf( //(1)!\n    typeOf&lt;ExampleData&gt;(),\n    typeOf&lt;OtherData&gt;()\n))\n</code></pre> <ol> <li>The schema of the body is either an <code>ExampleData</code> or an <code>OtherData</code>.</li> </ol> Nested Composite Schemas <p>The operations <code>array</code> and <code>anyOf</code> as well as <code>ref</code> and <code>type</code> can be combined and nested to create more complex variations.</p> <pre><code>body(\n    anyOf(\n        array&lt;String&gt;(),\n        anyOf(\n            ref(\"my-schema\"),\n            type&lt;MyExampleData&gt;()\n        ),        \n    )\n)\n</code></pre>"},{"location":"openapi/handling_types_schemas_examples/#local-examples","title":"Local Examples","text":"<p>Usually, examples for request and response bodies, headers, parameters, etc. are specified directly at the route and then automatically collected during generation of the OpenAPI specification. Example values can be specified as normal Kotlin/Java objects that get transformed to \"json\" and added to the OpenAPI specification.</p> <pre><code>body&lt;ExampleData&gt; { //(1)!\n    example(\"First Example\") {\n        value = ExampleData()\n        description = \"...\"\n        summary = \"...\"\n    }\n    example(\"Second Example\") {\n        //...\n    }\n}\n</code></pre> <ol> <li>A body is used as an example here. It works the same for parameters, headers, etc.</li> </ol>"},{"location":"openapi/handling_types_schemas_examples/#global-examples","title":"Global Examples","text":"<p>Global or \"shared\" examples can be defined in the examples section of the plugin configuration and are placed in the components/examples section of the final OpenAPI specification.</p> Defining Global Examples <pre><code>install(OpenApi) {\n    examples {\n        example(\"first-global\") {\n            description = \"first global example\"\n            value = ExampleData()\n        }\n        example(\"second-global\") {\n            description = \"second global example\"\n            value = ExampleData()\n        }\n        //...\n    }\n}\n</code></pre> More Information <p> API Reference</p> <p>Global examples can then be referenced by any route documentation by their ids.</p> Referencing Global Examples <pre><code>body&lt;ExampleData&gt; { //(2)!\n    exampleRef(\"Example 1\", \"first-global\") //(2)!\n    exampleRef(\"Example 2\", \"second-global\")\n    //...\n}\n</code></pre> <ol> <li>A body is used as an example here. It works the same for parameters, headers, etc.</li> <li>Reference the global example with id <code>first-global</code> and add it with the name <code>Example 1</code>.</li> </ol>"},{"location":"openapi/multiple_specs/","title":"Multiple OpenAPI Specifications","text":"<p>The OpenAPI plugin support generating multiple OpenAPI specifications from a single application. This can be useful for example when versioning endpoints or separate internal and external APIs.</p> <p>Each specification has a unique name used to configure the specification and assign routes to it.</p>"},{"location":"openapi/multiple_specs/#configuring-specifications","title":"Configuring Specifications","text":"<p>Specifications do not need to be mentioned in the plugin configuration to function. The normal plugin config serves as a base for all specs, which can then be overwritten. The available configuration options for specific OpenAPI specifications are the same as for the base plugin configuration. </p> <pre><code>install(OpenAPI) {\n    info {\n        title = \"Example API\" //(1)!\n    }\n    spec(\"version1\") { //(2)!\n        info {\n            version = \"1.0\" //(3)!\n        }\n    }\n    spec(\"version2\") { //(4)!\n        info {\n            version = \"2.0\" //(5)!\n        }\n    }\n}\n</code></pre> <ol> <li>Base plugin configuration defining the title of the api.</li> <li>Define configuration for a specification with name <code>version1</code>. The specification inherits the configuration from the base plugin config and further modifications added here will only affect the specification with this name while overwriting the base configuration.</li> <li>Set the version of the specification with name <code>version1</code> to \"1.0\".</li> <li>Define configuration for a specification with name <code>version2</code>. The specification inherits the configurations from the base plugin config and further modifications added here will only affect the specification with this name while overwriting the base configuration.</li> <li>Set the version of the specification with name <code>version2</code> to \"2.0\".</li> </ol>"},{"location":"openapi/multiple_specs/#assigning-routes","title":"Assigning Routes","text":"<p>Routes can be assigned to specifications either via a flag at the route documentation or via a function in the plugin configuration.</p> Route DocumentationPlugin Configuration <p>A single route can be assigned to a specific OpenAPI specification by setting the flag to the name of the specification.</p> <pre><code>get(\"example\", {\n    specName = \"version1\" //(1)!\n}) { /*...*/ }\n</code></pre> <ol> <li>Assigns the route <code>/example</code> to a specification with name <code>version1</code>.</li> </ol> <p>This can also be done for a whole group of routes by adding it to a higher up route</p> <pre><code>route(\"v1\", {\n    specName = \"version1\" //(1)!\n})  {\n    get(\"example\", {}) { /*...*/ }\n    post(\"example\", {}) { /*...*/ }\n    delete(\"example\", {}) { /*...*/ }\n}\n</code></pre> <ol> <li>Assigns the route <code>/v1</code> and all child routes to a specification with name <code>version1</code>.</li> </ol> <p>Routes can be assigned to specific OpenAPI specifications via an \"assigner\"-function in the plugin configuration that takes the url and tags of a route and returns the name of the specification to assign that route to. This function will only be used for routes that are not already assigned to a specification by other means. </p> <pre><code>install(OpenApi) {\n    specAssigner = { url, tags -&gt; url.firstOrNull() ?: \"default\" } //(1)!\n}\n</code></pre> <ol> <li>Assigns all (still unassigned) routes to the specification with the name equal to the first part in the url or the route (or \"default\" for root-routes).</li> </ol>"},{"location":"openapi/multiple_specs/#serving-multiple-openapi-specifications","title":"Serving Multiple OpenAPI Specifications","text":"<p>Routes providing the different specifications have to be created for each one.</p> <pre><code>routing {\n    route(\"v1/api.json\") {\n        openApi(\"version1\") //(1)!\n    }\n    route(\"v2/api.json\") {\n        openApi(\"version2\") //(2)!\n    }\n}\n</code></pre> <ol> <li>Provide the OpenAPI specification with name <code>version1</code> at <code>/v1/api.json</code></li> <li>Provide the OpenAPI specification with name <code>version2</code> at <code>/v2/api.json</code></li> </ol>"},{"location":"openapi/multiple_specs/#combine-with-swagger-ui-and-redoc","title":"Combine with Swagger UI and ReDoc","text":"<p>When using the Swagger UI or ReDoc library, handling multiple OpenAPI specifications is straight forward.</p> Swagger UI (multiple)Swagger UI (single)ReDoc <p>Multiple OpenAPI specifications can be served in multiple \"instances\" of Swagger UI</p> <pre><code>routing {\n    route(\"v1\") {\n        route(\"api.json\") {\n            openApi(\"version1\") //(1)!\n        }\n        route(\"swagger\") {\n            swaggerUI(\"/v1/api.json\") //(2)!\n        }\n    }\n    route(\"v2\") {\n        route(\"api.json\") {\n            openApi(\"version2\") //(3)!\n        }\n        route(\"swagger\") {\n            swaggerUI(\"/v2/api.json\") //(4)!\n        }\n    }\n}\n</code></pre> <ol> <li>Provide OpenAPI specification with name <code>version1</code> at <code>/v1/api.json</code></li> <li>Serve Swagger UI at <code>/v1/swagger</code> with specification from <code>/v1/api.json</code></li> <li>Provide OpenAPI specification with name <code>version2</code> at <code>/v2/api.json</code></li> <li>Serve Swagger UI at <code>/v2/swagger</code> with specification from <code>/v2/api.json</code></li> </ol> <p>Multiple OpenAPI specifications can be served in the same Swagger UI and browsed via a dropdown selection.</p> <pre><code>routing {\n    route(\"v1/api.json\") {\n        openApi(\"version1\") //(1)!\n    }\n    route(\"v2/api.json\") {\n        openApi(\"version2\") //(2)!\n    }\n    route(\"swagger\") {\n        swaggerUI(mapOf( //(3)!\n            \"Version 1\" to \"/v1/api.json\",\n            \"Version 2\" to \"/v2/api.json\",\n        ))\n    }\n}\n</code></pre> <ol> <li>Provide the OpenAPI specification with name <code>version1</code> at <code>/v1/api.json</code></li> <li>Provide the OpenAPI specification with name <code>version2</code> at <code>/v2/api.json</code></li> <li>Serve a Swagger UI at <code>/swagger</code> with both OpenAPI specification selectable. <code>Version 1</code> and <code>Version 2</code> are the names with which they are shown in the ui.</li> </ol> <p>Multiple OpenAPI specifications can be served in multiple \"instances\" of ReDoc</p> <pre><code>routing {\n    route(\"v1\") {\n        route(\"api.json\") {\n            openApi(\"version1\") //(1)!\n        }\n        route(\"redoc\") {\n            redoc(\"/v1/api.json\") //(2)!\n        }\n    }\n    route(\"v2\") {\n        route(\"api.json\") {\n            openApi(\"version2\") //(3)!\n        }\n        route(\"redoc\") {\n            redoc(\"/v2/api.json\") //(4)!\n        }\n    }\n}\n</code></pre> <ol> <li>Provide OpenAPI specification with name <code>version1</code> at <code>/v1/api.json</code></li> <li>Serve ReDoc at <code>/v1/redoc</code> with specification from <code>/v1/api.json</code></li> <li>Provide OpenAPI specification with name <code>version2</code> at <code>/v2/api.json</code></li> <li>Serve ReDoc at <code>/v2/redoc</code> with specification from <code>/v2/api.json</code></li> </ol>"},{"location":"openapi/plugin_configuration/","title":"Plugin Configuration","text":"More Information <p>The complete list of all available plugin configuration options can be viewed in the API Reference.</p>"},{"location":"openapi/plugin_configuration/#automatically-assigning-tags","title":"Automatically Assigning Tags","text":"<p>Tags can either be assigned at the route documentation directly or via a mapping function in the plugin configuration. The specified function is called for each route and the returned tags are added to the route's documentation.</p> <pre><code>install(OpenApi) {\n    tags {\n        tagGenerator = { url -&gt; TODO() }\n    }\n}\n</code></pre>"},{"location":"openapi/plugin_configuration/#ignoring-route-selectors","title":"Ignoring Route Selectors","text":"<p>Other Ktor plugins may add additional route selectors to routes that show up the OpenAPI specification as unwanted parts in the urls. These can be ignored by adding them to the <code>ignoredRouteSelectors</code> or <code>ignoredRouteSelectorClassNames</code> configuration.</p> Ignoring Route Selector <pre><code>install(OpenApi) {\n    tags {\n        ignoredRouteSelectors = ignoredRouteSelectors + RateLimitRouteSelector::class//(1)!\n        ignoredRouteSelectorClassNames = ignoredRouteSelectorClassNames + \"io.ktor.server.plugins.ratelimit.RateLimitRouteSelector\"//(2)!\n    }\n}\n</code></pre> <ol> <li>Ignore the route selector from the Ktor Rate Limiting Plugin by adding its class to the ignored selectors.</li> <li>Ignore the route selector from the Ktor Rate Limiting Plugin by adding its qualified name to the ignored selector names. This can be useful when the actual class is internal and cannot be accessed.</li> </ol>"},{"location":"openapi/plugin_configuration/#excluding-routes","title":"Excluding Routes","text":"<p>Routes can be completely excluded from the generated OpenAPI specification via the <code>hidden</code>-flag at the route documentation or the <code>pathFilter</code>-function in the plugin configuration.</p> Excluding Routes Route DocumentationPlugin Configuration <pre><code>routing {\n    get({\n        hidden = true //(1)!\n    }) {\n        call.respond(HttpStatusCode.NotImplemented, Unit)\n    }\n}\n</code></pre> <ol> <li>Mark this route as \"hidden\" and exclude it from the generated OpenAPI specification.</li> </ol> <pre><code>install(OpenApi) {\n    pathFilter = { method, url -&gt; url.firstOrNull() != \"internal\" }//(1)!\n}\n</code></pre> <ol> <li>Filter out and exclude all routes with urls starting with \"internal\". The function receives the http method and url as inputs and returns <code>false</code> for excluded routes.</li> </ol>"},{"location":"openapi/plugin_configuration/#choosing-the-output-format-json-or-yaml","title":"Choosing the output format - JSON or YAML","text":"<p>The output format of the generated OpenAPI specification can be changed from <code>json</code> to <code>yaml</code> with the <code>outputFormat</code> option.</p> Choosing the Output Format JSONYAML <pre><code>install(OpenApi) {\n    outputFormat = OutputFormat.JSON\n}\n</code></pre> <pre><code>install(OpenApi) {\n    outputFormat = OutputFormat.YAML\n}\n</code></pre>"},{"location":"openapi/plugin_configuration/#customizing-schemas","title":"Customizing Schemas","text":"<p> Schema Generation</p>"},{"location":"openapi/plugin_configuration/#customizing-examples","title":"Customizing Examples","text":"<p> Example Encoding</p>"},{"location":"openapi/plugin_configuration/#configuring-multiple-openapi-specifications","title":"Configuring Multiple OpenAPI Specifications","text":"<p> Multiple API Specifications</p>"},{"location":"openapi/schema_generation/","title":"Schema Generation","text":"<p>Schemas are automatically generated from types using a schema generation function configured in the plugin configuration and uses schema-kenerator by default.</p>"},{"location":"openapi/schema_generation/#pre-defined-schema-generators","title":"Pre-Defined Schema Generators","text":"ReflectionKotlinx.Serialization <p>The (default) schema generator using reflection to analyze types and produce schemas. The result is closest to the result produced by serializers like Jackson.</p> <pre><code>install(OpenApi) {\n    schemas {\n        generator = SchemaGenerator.reflection { //(1)!\n            //...(2)\n        }\n    }\n}\n</code></pre> <ol> <li>Use the pre-defined schema generator using reflection to automatically generate schemas from types.</li> <li>Configure the schema generator (optional).</li> </ol> More Information <p> API Reference</p> <p>Schema generator using kotlinx.serialization to analyze types and produce schemas. </p> <pre><code>install(OpenApi) {\n    schemas {\n        generator = SchemaGenerator.kotlinx { //(1)!\n            //...(2)\n        }\n    }\n}\n</code></pre> <ol> <li>Use the pre-defined schema generator using kotlinx.serialization to automatically generate schemas from types.</li> <li>Configure the schema generator (optional).</li> </ol> More Information <p> API Reference</p>"},{"location":"openapi/schema_generation/#defining-custom-schemas-for-types","title":"Defining Custom Schemas for Types","text":"<p>Sometimes the schema generator does not produce the exact desired result for some types. See schema-kenerator for detailed information on how to modify schemas for types.</p> <p>When using the pre-build schema generators, \"Type Overwrites\" can be used to completely replace the schema for a type with another schema.</p> ReflectionKotlinx.Serialization <pre><code>install(OpenApi) {\n    schemas {\n        generator = SchemaGenerator.reflection {\n            overwrite(SchemaGenerator.TypeOverwrites.LocalDateTime()) //(1)!\n            overwrite(SchemaGenerator.TypeOverwrites.File()) //(2)!\n            //...\n        }\n    }\n}\n</code></pre> <ol> <li>Use a pre-defined schema for <code>java.time.LocalDateTime</code> instead of the auto-generated one.</li> <li>Use a pre-defined schema for <code>java.io.File</code> instead of the auto-generated one.</li> </ol> <pre><code>install(OpenApi) {\n    schemas {\n        generator = SchemaGenerator.kotlinx {\n            overwrite(SchemaGenerator.TypeOverwrites.LocalDateTime()) //(1)!\n            overwrite(SchemaGenerator.TypeOverwrites.File()) //(2)!\n            //...\n        }\n    }\n}\n</code></pre> <ol> <li>Use a pre-defined schema for <code>java.time.LocalDateTime</code> instead of the auto-generated one.</li> <li>Use a pre-defined schema for <code>java.io.File</code> instead of the auto-generated one.</li> </ol> More Information <p> API Reference for available type overwrites.</p> <p> API Reference for interface to implement own type overwrites.</p>"},{"location":"openapi/schema_generation/#custom-schema-generators","title":"Custom Schema Generators","text":"<p>The schema generator can be completely replaced by an own implementation or by a different, non pre-built configuration.</p> <pre><code>schemas {\n    generator = { type -&gt; //(1)!\n        TODO() //(2)!\n    }\n}\n</code></pre> <ol> <li>Input of the schema generation function is the type either as a (wrapped) <code>KType</code> or <code>SerialDescriptor</code>.</li> <li>Generate the schema and return the result as a <code>io.github.smiley4.schemakenerator.swagger.data.CompiledSwaggerSchema</code></li> </ol> Custom schema-kenerator \"pipeline\" <p>Using schema-kenerator to build a custom schema generation \"pipeline\".</p> <pre><code>schemas {\n    generator = { type -&gt;\n        type\n            .processReflection()\n            .generateSwaggerSchema()\n            .withTitle(TitleType.SIMPLE)\n            .compileReferencingRoot()\n    }\n}\n</code></pre> More Information <p>More information can be found in the wiki of the schema-kenerator project together with an overview of the additional required dependencies.</p>"},{"location":"openapi/typesafe_routing/","title":"Type-safe Routing","text":"<p>This project supports documenting Ktor routes defined using Type-safe Routing.</p> Documenting Routes <pre><code>@Resource(\"{id}\")\nclass Entity( //(1)!\n    val id: String\n)\n</code></pre> <ol> <li>Define resources classes as usual. </li> </ol> <pre><code>import io.github.smiley4.ktoropenapi.resources.get //(1)!\n\nrouting {\n    get&lt;Entity&gt;({ //(2)!\n        description = \"Returns the entity with the requested id.\"\n    }) {\n        call.respond(HttpStatusCode.NotImplemented, Unit) //(3)!\n    }\n}\n</code></pre> <ol> <li>Replace <code>io.ktor.server.resources.get</code> with <code>io.github.smiley4.ktoropenapi.resources.get</code>. Same for other http methods.</li> <li>Define the route using the resource class and add additional documentation.</li> <li>Handle requests as usual.</li> </ol> More Information <p>See Documenting Routes for more general information.</p> Extracting Information from Resource Classes <p>When using type-safe routing, information about the routes can be extracted from the resources classes. This includes available query and path parameters with their names, types and whether they are optional. All other information still needs to be added manually and at the routes.</p> <p>To enable automatic extraction, set the <code>autoDocumentResourcesRoutes</code> option in the plugin configuration.</p> <pre><code>install(OpenApi) {\n    autoDocumentResourcesRoutes = true\n}\n</code></pre> Schema Generation <p>When using Type-safe routing and <code>autoDocumentResourcesRoutes</code> is enabled, schemas must be generated using kotlinx.serialization. See Schema Generation for more information on how to change the default generator.</p> <pre><code>install(OpenApi) {\n    // enable automatically extracting documentation from resources-routes\n    autoDocumentResourcesRoutes = true\n    // schema-generator must use kotlinx-serialization to be compatible\n    schemas {\n        generator = SchemaGenerator.kotlinx()\n    }\n}\n</code></pre>"},{"location":"redoc/","title":"ReDoc","text":"<p>Library for Ktor applications to serve ReDoc - visualize and interact with generated OpenAPI specifications.</p>"},{"location":"redoc/#features","title":"Features","text":"<ul> <li>Explore and interact with OpenAPI specifications generated by <code>ktor-openapi</code> or external specifications</li> <li>Serve bundled ReDoc page</li> <li>Expose multiple \"instances\" of ReDoc (e.g. for different OpenAPI specifications)</li> <li>All ReDoc configuration options available</li> </ul>"},{"location":"redoc/#example","title":"Example","text":"<pre><code>routing {\n\n    route(\"redoc\") { //(1)!\n        redoc(\"/api.json\") { //(2)!\n            //...(3)\n        }\n    }\n\n}\n</code></pre> <ol> <li>Specify route to serve ReDoc at <code>/redoc</code>.</li> <li>Expose ReDoc showing OpenAPI specification at <code>/api.json</code>. Path can be relative pointing to specification provided by this application or absolute pointing to an external resource.</li> <li>Add configuration for this ReDoc \"instance\" here.</li> </ol>"},{"location":"redoc/getting_started/","title":"Getting Started","text":""},{"location":"redoc/getting_started/#add-dependency","title":"Add Dependency","text":"<p>To serve ReDoc, you need to include the <code>ktor-redoc</code> artifact in the build script. All artifacts are published to Maven Central.</p> Gradle (Kotlin)GradleMaven <pre><code>implementation(\"io.github.smiley4:ktor-redoc:$version\")\n</code></pre> <pre><code>implementation 'io.github.smiley4:ktor-redoc:$version'\n</code></pre> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;io.github.smiley4&lt;/groupId&gt;\n    &lt;artifactId&gt;ktor-redoc&lt;/artifactId&gt;\n    &lt;version&gt;${version}&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre>"},{"location":"redoc/getting_started/#usage","title":"Usage","text":"<pre><code>routing {\n\n    route(\"redoc\") { //(1)!\n        redoc(\"/api.json\") { //(2)!\n            //...(3)\n        }\n    }\n\n}\n</code></pre> <ol> <li>Specify route to serve ReDoc at <code>/redoc</code>.</li> <li>Expose ReDoc showing the OpenAPI specification at <code>/api.json</code>. The url can be relative pointing to specification provided by this application or absolute pointing to an external resource.</li> <li>Add configuration for this ReDoc \"instance\" here.</li> </ol> Using ReDoc with auto-generated OpenAPI specification <pre><code>routing {\n\n    route(\"api.json\") {\n        openApi() //(1)!\n    }\n    route(\"redoc\") {\n        redoc(\"/api.json\") //(2)!\n    }\n\n}\n</code></pre> <ol> <li>Serve auto-generated OpenAPI specification at <code>/api.json</code>.</li> <li>Expose ReDoc using auto-generated specification at <code>/api.json</code>.</li> </ol> Configuration Options <p>For more information on available configuration options, please see the api reference.</p>"},{"location":"swaggerui/","title":"Swagger UI","text":"<p>Library for Ktor applications to serve Swagger UI - visualize and interact with generated OpenAPI specifications.</p>"},{"location":"swaggerui/#features","title":"Features","text":"<ul> <li>Explore and interact with OpenAPI specifications generated by <code>ktor-openapi</code> or external specifications</li> <li>Serve bundled Swagger UI</li> <li>Expose multiple \"instances\" of Swagger UI (e.g. for different OpenAPI specifications)</li> <li>All Swagger UI configuration options available</li> </ul>"},{"location":"swaggerui/#example","title":"Example","text":"<pre><code>routing {\n\n    route(\"swagger\") { //(1)!\n        swaggerUI(\"/api.json\") { //(2)!\n            //...(3)\n        }\n    }\n\n}\n</code></pre> <ol> <li>Specify route to serve Swagger UI at <code>/swagger</code>.</li> <li>Expose Swagger UI using OpenAPI specification at <code>/api.json</code>. Path can be relative pointing to specification provided by this application or absolute pointing to an external resource.</li> <li>Add configuration for this Swagger UI \"instance\" here.</li> </ol>"},{"location":"swaggerui/getting_started/","title":"Getting Started","text":""},{"location":"swaggerui/getting_started/#add-dependency","title":"Add Dependency","text":"<p>To serve Swagger UI, you need to include the <code>ktor-swagger-ui</code> artifact in the build script. All artifacts are published to Maven Central.</p> Gradle (Kotlin)GradleMaven <pre><code>implementation(\"io.github.smiley4:ktor-swagger-ui:$version\")\n</code></pre> <pre><code>implementation 'io.github.smiley4:ktor-swagger-ui:$version'\n</code></pre> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;io.github.smiley4&lt;/groupId&gt;\n    &lt;artifactId&gt;ktor-swagger-ui&lt;/artifactId&gt;\n    &lt;version&gt;${version}&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre>"},{"location":"swaggerui/getting_started/#usage","title":"Usage","text":"<pre><code>routing {\n\n    route(\"swagger\") { //(1)!\n        swaggerUI(\"/api.json\") { //(2)!\n            //...(3)\n        }\n    }\n\n}\n</code></pre> <ol> <li>Specify route to serve Swagger UI at <code>/swagger</code>.</li> <li>Expose Swagger UI showing OpenAPI specification at <code>/api.json</code>. The url can be relative pointing to specification provided by this application or absolute pointing to an external resource. Specify a map of urls to interact with multiple different specifications in the same Swagger UI \"instance\".</li> <li>Add configuration for this Swagger UI \"instance\" here.</li> </ol> Using Swagger UI with auto-generated OpenAPI specification <pre><code>routing {\n\n    route(\"api.json\") {\n        openApi() //(1)!\n    }\n    route(\"swagger\") {\n        swaggerUI(\"/api.json\") //(2)!\n    }\n\n}\n</code></pre> <ol> <li>Serve auto-generated OpenAPI specification at <code>/api.json</code>.</li> <li>Expose Swagger UI using auto-generated specification at <code>/api.json</code>.</li> </ol> Configuration Options <p>For more information on available configuration options, please see the api reference.</p>"}]}